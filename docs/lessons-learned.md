# Lessons Learned — ImmoCrew

## Session 5 — 2026-03-26

### Learnings

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Statut |
|---------|------|-----------|----------|-------------|---------------------|-------------------------|--------|
| S5 | 2026-03-26 | 2. Insistance utilisateur | P0 | Accents manquants signalés 3+ fois. Les unicode escapes (\u00E9) et HTML entities (&eacute;) dans les constantes JS ne sont pas toujours rendus — les vrais caractères UTF-8 sont la seule solution fiable. | Passe globale python3 pour remplacer tous les \uXXXX par des vrais caractères. | Ajouter une règle dans CLAUDE.md : "Dans les fichiers TSX/JSX, utiliser les vrais caractères UTF-8 (é, è, à, ç) dans les constantes JavaScript. Ne JAMAIS utiliser \u00E9 ni &eacute; dans les strings JS. Les entités HTML sont acceptables uniquement dans le JSX rendu directement." | appliqué |
| S5 | 2026-03-26 | 1. Problème rencontré | P0 | Les prix (497/197/97) étaient hardcodés dans 15+ endroits (Pricing, CGV, FAQ, metadata, JSON-LD, checkout tracking, tests). Un changement de prix nécessite une passe globale. | Grep + remplacement dans tous les fichiers. | Centraliser les prix dans un fichier de config unique (ex: src/lib/pricing.ts) et les importer partout. Jamais de prix hardcodé dans un composant. | appliqué |
| S5 | 2026-03-26 | 8. Préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] : Thomas refuse le charm pricing "en 7" (497/197/97) car incohérent avec le positionnement "zero bullshit". Préfère les prix ronds (400/150/100) même si le MRR cible augmente de 30 à 40 clients. La cohérence de marque prime sur l'optimisation tarifaire. | Prix ronds appliqués partout. | — | appliqué |
| S5 | 2026-03-26 | 8. Préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] : Thomas veut JAMAIS de mention de concurrent par nom sur le site (pas de "Cocoon-Immo", pas de nom de service concurrent). Les comparaisons doivent utiliser des catégories ("freelance marketing", "outil avec templates", "plateforme SaaS"). | Toutes les mentions supprimées. | Ajouter dans CLAUDE.md une règle absolue : "Ne JAMAIS mentionner de concurrent par nom dans le code, les composants, ou les contenus visibles par le client." | appliqué |
| S5 | 2026-03-26 | 8. Préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] : Thomas préfère NextAuth.js à Clerk — l'auth locale (zero dépendance externe) est préférable même si Clerk est plus rapide à configurer. La raison : Clerk causait des erreurs serveur, la dépendance externe ajoutait un point de défaillance. | Migration complète Clerk → NextAuth (18 fichiers). | — | appliqué |
| S5 | 2026-03-26 | 8. Préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] : Thomas veut que le site soit "au niveau" — refus systématique de tout ce qui est < 9/10. A demandé des itérations successives jusqu'à 9.5/10 sur chaque section. Le seuil de qualité est non-négociable. | Audits section par section avec 4 agents en parallèle, corrections jusqu'à 9/10 partout. | — | appliqué |
| S5 | 2026-03-26 | 8. Préférence fondateur | P0 | [PRÉFÉRENCE FONDATEUR] : Thomas veut un modal popup pour l'auth (pas une page pleine). Les pages /sign-in et /sign-up doivent garder le Header + Footer pour la cohérence de navigation. Sophie ne doit jamais perdre ses repères. | AuthModal créé, pages sign-in/sign-up avec Header+Footer. | — | appliqué |
| S5 | 2026-03-26 | 8. Préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] : Thomas refuse la duplication d'info — si Sophie donne son prénom/nom à l'inscription, l'onboarding ne doit PAS les redemander. Le pré-remplissage est obligatoire. | useEffect pré-remplit depuis la session NextAuth. | — | appliqué |
| S5 | 2026-03-26 | 8. Préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] : Thomas préfère demander un lien LinkedIn plutôt que 3 champs texte pour l'histoire personnelle. Sophie ne racontera jamais son parcours entre deux visites — un lien LinkedIn est plus réaliste. | Étape "Ton histoire" → "Ton profil" avec LinkedIn URL + bio optionnelle. | — | appliqué |
| S5 | 2026-03-26 | 8. Préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] : Thomas veut que le flow soit validé par l'admin avant l'automatisation complète. La production de livrables reste déclenchée manuellement depuis /admin pour l'instant. L'auto-produce est prêt mais désactivé. | Route /api/auto-produce créée mais non appelée automatiquement. | — | appliqué |
| S5 | 2026-03-26 | 8. Préférence fondateur | P0 | [PRÉFÉRENCE FONDATEUR] : Thomas veut que si Sophie résilie, elle perde l'accès à ses livrables. Les livrables sont liés à l'abonnement actif, pas acquis à vie. | Dashboard affiche message "abonnement terminé" + CTA réabonnement pour les clients churned. | — | appliqué |
| S5 | 2026-03-26 | 5. Pattern efficace | P1 | L'audit section par section avec 4 agents en parallèle (@creative-strategy, @copywriter, @design, @ux) puis consolidation est le pattern le plus efficace pour atteindre 9.5/10. Chaque agent attrape des problèmes que les autres manquent. | — | Documenter ce pattern comme "Audit chirurgical" dans l'orchestrator. | ouvert |
| S5 | 2026-03-26 | 5. Pattern efficace | P2 | Le flow "audit → corrections → re-audit" en boucle est efficace mais coûteux en tokens. Un seul cycle audit + corrections + QA final est optimal. Le re-audit systématique n'est justifié que si le score initial est < 7/10. | — | — | ouvert |
| S5 | 2026-03-26 | 3. Requête non couverte | P1 | Pas de protocole pour "migrer un service d'auth" (Clerk → NextAuth). La migration a touché 18 fichiers et nécessité un audit QA complet. Un guide de migration auth serait utile. | — | Créer un guide @fullstack "Migration auth provider" avec checklist des fichiers à modifier. | ouvert |
| S5 | 2026-03-26 | 4. Biais détecté | P1 | Le "48h de délai de livraison" était un vestige du mindset humain (Phase 2). Avec un pipeline IA automatisé, les livrables sont générés en minutes. Le message a été corrigé en "sous 24h" pour rester honnête avec le flow admin-validé actuel. | Messages mis à jour. | — | appliqué |

## Session 6 — 2026-03-27

### Learnings

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Statut |
|---------|------|-----------|----------|-------------|---------------------|-------------------------|--------|
| S6 | 2026-03-27 | 1. Problème rencontré | P0 | Migration SQL initiale manquante — tables clients, deliverables, payments jamais créées. Les migrations 003+ font des ALTER sur des tables inexistantes. Cause de "Erreur lors de la sauvegarde". | Créé sql/001_init.sql avec CREATE TABLE IF NOT EXISTS pour les 5 tables + index. | Toujours vérifier qu'une migration 001 (CREATE TABLE) existe avant de produire des migrations ALTER. | appliqué |
| S6 | 2026-03-27 | 8. Préférence fondateur | P0 | [PRÉFÉRENCE FONDATEUR] : Thomas veut que /admin soit accessible par mot de passe seul, pas via NextAuth. L'admin ne devrait pas nécessiter de créer un compte. | Migration complète : admin auth par cookie ADMIN_PASSWORD, suppression de la dépendance NextAuth pour /admin. | — | appliqué |
| S6 | 2026-03-27 | 8. Préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] : Thomas refuse que Sophie remplisse des données que le système peut récupérer automatiquement. "On devrait le savoir à partir de la localisation." L'étape quartier (prix m2, commerces, écoles, transports) est inutile. | Supprimé étape 6 onboarding, enrichissement auto via API DVF + API Adresse à partir de ville/quartiers. | Ne jamais demander à l'utilisateur une donnée récupérable automatiquement via API publique. | appliqué |
| S6 | 2026-03-27 | 2. Insistance utilisateur | P1 | LinkedIn demandé 2 fois dans l'onboarding (étape "Ton profil" + étape "Tes comptes"). Duplication d'info = friction. | Supprimé linkedin de l'étape "Tes comptes", linkedin_url auto-synced vers le champ réseau social. | — | appliqué |
| S6 | 2026-03-27 | 8. Préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] : Le lien d'annonce existante (SeLoger, LeBonCoin) devrait être le champ principal quand Sophie ajoute un bien, pas un champ optionnel en bas. Les champs manuels sont secondaires. | Lien annonce déplacé en premier (encadré orange), champs manuels en "ou remplis manuellement". | — | appliqué |
| S6 | 2026-03-27 | 1. Problème rencontré | P1 | Next.js 14.2+ requiert params comme Promise (await) dans les pages dynamiques. Les pages /admin/clients/[id] et /bien/[id] crashaient car params.id accédé directement. | Corrigé avec `const { id } = await params` dans les 2 pages. blog/[slug] était déjà correct. | Vérifier systématiquement que toutes les pages dynamiques utilisent `params: Promise<{...}>` + await. | appliqué |
| S6 | 2026-03-27 | 1. Problème rencontré | P1 | Migration 006_password_reset.sql déclarait user_id UUID mais clients.id est TEXT. La FK aurait planté à l'exécution. | Corrigé user_id UUID → TEXT dans 006. Consolidé dans 001_init.sql. | Toujours vérifier le type de la PK référencée avant de déclarer une FK. | appliqué |
| S6 | 2026-03-27 | 2. Insistance utilisateur | P0 | Le fondateur a signalé 5+ fois des erreurs en production que l'équipe QA n'avait pas détectées avant de pousser. Problèmes : (1) colonnes SQL manquantes dans les migrations (title, content, type sur deliverables), (2) ON CONFLICT partiel non matché, (3) self-fetch via proxy Replit qui timeout, (4) TypeScript ReactNode errors sur ClientContextCard — 4 tentatives pour corriger. Cause racine : AUCUNE vérification en production avant de dire "c'est corrigé". | Ajout de `tsc --noEmit` systématique avant chaque push. Migration 008 corrigée avec ALTER COLUMN pour TOUTES les colonnes. Self-fetch via localhost. | **RÈGLE ABSOLUE À AJOUTER** : avant de confirmer qu'un fix est déployable, l'agent QA ou fullstack DOIT : (1) lancer `tsc --noEmit` et vérifier 0 erreurs, (2) pour les migrations SQL, vérifier que CHAQUE colonne utilisée dans le code a un ALTER TABLE ADD COLUMN IF NOT EXISTS correspondant (pas juste les colonnes "ajoutées progressivement"), (3) pour les appels réseau internes (self-fetch), tester avec `curl` que l'URL est accessible, (4) NE JAMAIS dire "c'est corrigé" sans avoir exécuté au moins une vérification automatisée. | ouvert |
| S6 | 2026-03-27 | 1. Problème rencontré | P0 | Le self-fetch dans trigger-production passait par le proxy externe Replit (via Host header) qui a un timeout de ~30-60s. Un pack lancement = 7 appels Claude = 3-7 min → le proxy coupe → retourne "upstream request timeout" en HTML → response.json() crash "Unexpected token 'u'". | Fix : URL interne `http://127.0.0.1:PORT`, timeout 10 min, parsing text+JSON avec fallback. | Tout self-fetch Next.js DOIT utiliser localhost (http://127.0.0.1:PORT) et JAMAIS l'URL publique. Les reverse proxies (Replit, Vercel, Cloudflare) ont des timeouts incompatibles avec les requêtes longues. | appliqué |
| S6 | 2026-03-27 | 1. Problème rencontré | P0 | Le script SQL 008_definitive_fix.sql faisait CREATE TABLE IF NOT EXISTS + ALTER TABLE ADD COLUMN seulement pour les colonnes "ajoutées progressivement". Les colonnes de base (title, content, type, status sur deliverables) n'avaient pas de ALTER. Si la table existait avec un schéma minimal, ces colonnes manquaient. | Ajouté ALTER TABLE ADD COLUMN IF NOT EXISTS pour CHAQUE colonne de CHAQUE table. | Les scripts de migration "de convergence" DOIVENT avoir un ALTER TABLE ADD COLUMN pour CHAQUE colonne, même celles qui sont dans le CREATE TABLE. Le CREATE Table ne fait rien si la table existe déjà. | appliqué |
| S6 | 2026-03-27 | 8. Préférence fondateur | P0 | [PRÉFÉRENCE FONDATEUR] : Thomas trouve l'équipe "très très légère" sur la qualité. Il refuse la notion de MVP — le produit doit être de qualité production dès le départ. Les agents ont validé à 7/10 et 9/10 des livrables que Thomas juge inacceptables (carte profil = bloc bleu vide, dashboard = liste plate de cards sans visuels). Le seuil de qualité du fondateur est systématiquement plus élevé que celui des agents. | Multiple itérations : 3 passes sur la carte profil, 4 passes sur le dashboard, 5 passes sur le ClientContextCard TypeScript. | **RÈGLE ABSOLUE** : (1) Ne JAMAIS valider un composant UI sans screenshot mental — si c'est "un div coloré vide", c'est pas validé. (2) Les agents UI (@ux, @design) doivent se demander "est-ce que Sophie serait FIÈRE de montrer ça ?" avant de noter 9+/10. (3) Le fondateur exige 10/10 sur chaque critère, pas une moyenne de 9 — chaque critère doit être à 9+ individuellement. (4) Arrêter de dire "c'est corrigé" sans avoir vérifié visuellement. Les checks TypeScript ne suffisent pas — le rendu visuel compte autant que la compilation. | ouvert |
| S6 | 2026-03-27 | 4. Biais détecté | P0 | L'agent @mandataire (Sophie) est construit comme un ÉVALUATEUR DE LIVRABLES, pas comme une UTILISATRICE RÉELLE. Elle note des critères abstraits (clarté 8/10, ton 9/10) au lieu de se poser les vraies questions terrain : "comment je passe au mensuel ?", "comment j'ajoute un bien ?", "pourquoi mes annonces sont génériques alors que j'ai donné mes biens ?", "où est mon plan d'action ?". Résultat : Sophie a validé à 9.25/10 un dashboard qui ne couvrait pas la moitié des parcours utilisateur réels. Le fondateur a dû signaler 10+ problèmes que Sophie n'avait pas vus. | Ajout d'un audit 10 scénarios d'usage concrets. Mais le problème de fond reste : le persona ne "vit" pas assez le produit. | **RÈGLE PERSONA** : (1) L'agent @mandataire ne doit JAMAIS noter un écran sans avoir simulé AU MOINS 10 scénarios d'usage concrets (pas des critères abstraits). (2) Chaque scénario doit être une ACTION que Sophie ferait dans sa journée réelle (copier un post, ajouter un bien, passer au mensuel, signaler un problème, modifier sa zone). (3) Si un scénario est impossible (pas de bouton, pas de page), c'est un FAIL à 0/10 sur ce scénario — pas un "point d'amélioration". (4) Le persona doit aussi évaluer la VALEUR AJOUTÉE : est-ce que le dashboard me donne un plan d'action, ou juste du contenu brut ? Sophie ne veut pas une bibliothèque de fichiers — elle veut un coach marketing. | ouvert |
| S6 | 2026-03-27 | 8. Préférence fondateur | P0 | [PRÉFÉRENCE FONDATEUR] : Thomas exige que le dashboard soit un COACH MARKETING PERSONNALISÉ, pas une bibliothèque de fichiers. Le dashboard doit commencer par un résumé de ce qu'on a compris sur Sophie (zone, style, biens), puis un plan d'action structuré ("mets à jour tes bios", "publie 3 posts/semaine", "tourne tes vidéos") avec des liens vers les articles du blog comme conseils. La valeur ajoutée d'ImmoCrew n'est pas juste le contenu — c'est le coaching et la stratégie. | À implémenter : section "Ton plan d'action" en haut du dashboard. | Le dashboard client DOIT commencer par un plan stratégique personnalisé, pas par du contenu brut. Chaque section doit avoir un "pourquoi" et un "comment" (conseils, fréquence recommandée, liens articles blog). | ouvert |
| S6 | 2026-03-27 | 8. Préférence fondateur | P0 | [PRÉFÉRENCE FONDATEUR] : Thomas refuse le "gros bloc bleu" comme bannière de profil. Le profil doit être COMPACT et INLINE (avatar 64px + nom à côté, pas une bannière de 144px). Les infos métier en pills horizontales, pas en grille 4 colonnes de mini-cards. Tous les réseaux sociaux (pas juste LinkedIn — aussi Instagram, Facebook, site web). | Carte profil à réécrire en compact. | Les composants UI profil ne doivent JAMAIS utiliser de bannières plein-largeur vides. Un profil compact (avatar + nom + pills) est toujours préférable. | ouvert |
| S6 | 2026-03-27 | 2. Insistance utilisateur | P0 | Les audits QA et les validations agents testent le CODE mais pas les SCÉNARIOS D'USAGE. Sophie doit pouvoir : (1) passer du Pack Lancement au Mensuel depuis son dashboard, (2) ajouter un nouveau bien, (3) modifier ses infos (zone, réseau), (4) distinguer les contenus mois 1 vs mois 2, (5) contacter le support, (6) résilier. Aucun de ces scénarios n'était testé par @qa. Les agents validaient à 9/10 un dashboard qui ne couvrait pas la moitié des parcours utilisateur réels. | Ajout d'un audit Sophie avec 10 scénarios d'usage concrets. | **RÈGLE QA** : avant de valider un écran client, @qa et @mandataire DOIVENT tester au minimum 10 scénarios d'usage réalistes (pas juste "est-ce que ça compile" ou "est-ce que c'est joli"). Chaque scénario = une action concrète que Sophie ferait dans sa journée. Si un scénario est impossible (ex: pas de bouton pour passer au mensuel), c'est un FAIL même si le code est parfait. | ouvert |
| S6 | 2026-03-27 | 8. Préférence fondateur | P0 | [PRÉFÉRENCE FONDATEUR] : Thomas refuse catégoriquement la notion de "MVP" pour le dashboard client. "On est sur un produit de qualité qu'on veut mettre à disposition." Chaque écran doit être au niveau d'un SaaS premium (Notion, Linear, Stripe Dashboard), pas d'un prototype. Les composants génériques (listes plates, texte brut sans mise en forme, stats basiques) sont inacceptables. | Dashboard refondu de 0 : timeline posts, biens→annonces, profil premium, markdown rendu, sections par contexte. | Ne JAMAIS produire un écran avec des placeholders visuels (blocs colorés vides, listes plates sans catégorisation, texte brut sans rendu). Chaque composant UI doit avoir : (a) des visuels (icônes, dégradés, patterns), (b) une structure logique (pas de liste plate), (c) un feedback visuel sur chaque interaction. | appliqué |
